const blueBottleAPI = 'https://blue-bottle-api-test.herokuapp.com/v1';
const tokenURL = `${blueBottleAPI}/tokens`;
const dataURL = `${blueBottleAPI}/coffee_shops`;

export default {
    apiURL: blueBottleAPI,
    tokenURL,
    dataURL,
};
